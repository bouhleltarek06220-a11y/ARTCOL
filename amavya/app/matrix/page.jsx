import MatrixShell from "./MatrixShell";

export const metadata = {
  title: "AMAVYA — Enter the Matrix",
  description:
    "Une expérience immersive : voyage à travers le code, du Matrix vert au cœur doré d'AMAVYA.",
};

export default function MatrixPage() {
  return <MatrixShell />;
}
