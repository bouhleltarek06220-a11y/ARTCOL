"use client";

// Placeholder pour la section Offre — sera complétée en S4 du plan
// (offre 490 € setup + 149 €/mois, FAQ, risk reversal).
// On le rend invisible mais on garde l'id pour les ancres futures.
export default function Offre() {
  return <section id="offre" aria-hidden="true" className="hidden" />;
}
