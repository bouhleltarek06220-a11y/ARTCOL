# --- Cœur API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0,<7.0

# --- LLM ---
anthropic>=0.40,<1.0

# --- Embeddings ---
# Mode local (souveraineté) : modèle multilingue exécuté sur la machine.
sentence-transformers>=3.0,<4.0
# Mode API (qualité supérieure) : Voyage AI / autre fournisseur via httpx (pas de SDK lourd).

# --- Base vectorielle locale ---
# Accélération optionnelle de la recherche ANN ; fallback numpy si indisponible.
sqlite-vec>=0.1.3
numpy>=1.26,<3.0

# --- Réseau / parsing documents ---
httpx>=0.27,<1.0
pypdf>=4.2,<6.0
python-docx>=1.1,<2.0
markdown>=3.6,<4.0
beautifulsoup4>=4.12,<5.0
selectolax>=0.3,<1.0

# --- Robustesse / observabilité ---
tenacity>=8.4,<10.0
slowapi>=0.1.9,<1.0
loguru>=0.7,<1.0

# --- CLI / console ---
typer>=0.12,<1.0
rich>=13.7,<15.0

# --- Tests ---
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
